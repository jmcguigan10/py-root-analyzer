# Fast smoke-test style run. Paths are resolved relative to the current
# working directory, not this config file.
recursive = true
max_files = 4
include_all_cycles = false
sample_entries = 25
json_stdout = false
strict = false
